# 食欲粉碎机 Agent 工作指南

本文件用于指导在此项目中工作的 AI 编码代理。开始修改前，先阅读 `SPEC.md` 和 `README.md`；如本文件与 `SPEC.md` 冲突，以 `SPEC.md` 为准。

## 1. 项目定位

- 产品名称：食欲粉碎机。
- 产品性质：手机浏览器中的虚拟减肥针娱乐互动体验，不是医疗产品。
- 核心流程：欢迎页 → 选择剂量 → 选择模拟部位 → 连续长按 15 秒 → 成功反馈 → 本机记录与匿名分享。
- 当前技术栈：Next.js App Router、React、TypeScript 严格模式、Tailwind CSS。
- 部署方式：纯前端静态导出，不依赖常驻服务端、数据库或付费 API。
- 主要语言：用户界面使用简体中文。

## 2. 不可突破的产品边界

- 不提供真实医疗建议、剂量推荐、疗效判断、健康风险评估或注射教学。
- 不暗示虚拟体验可以降低食欲、减重或产生真实药效。
- 剂量仅用于娱乐界面和本机记录，不得出现“推荐剂量”“适合你”等文案。
- “痛”“没食欲了”等内容必须保持明显的夸张、游戏化语境。
- 欢迎页必须持续显示：`本网站仅供娱乐，不代表真实药效或医疗建议。`
- 不要求或收集姓名、邮箱、电话、账号、健康档案等身份信息。
- 不上传剂量、部位、完成时间或历史记录；数据只保存在当前浏览器。
- 分享图只能包含网站名称和连续打卡天数，不得包含剂量、部位、日期、昵称或设备信息。
- 不加入摄像头、身体识别、动作感应或身体检测功能。
- 不使用未经授权、来源不明或带第三方水印的图片、声音和字体。

若需求可能越过以上边界，先停止实现并向用户说明风险及需要确认的变更。

## 3. 代码与架构约定

- 页面和路由放在 `src/app`。
- 共享界面组件放在 `src/components`。
- 领域功能按模块放在 `src/features`，不要把业务逻辑全部堆在页面组件中。
- 公共类型放在 `src/types`；优先复用 `DoseMg`、`InjectionSite` 和 `ExperienceRecord`。
- 浏览器能力检测及降级逻辑放在 `src/lib/browser`。
- 使用 `@/*` 路径别名引用 `src` 下的模块。
- 保持 TypeScript 严格类型，不使用 `any` 绕过问题，不用无依据的类型断言掩盖错误。
- 默认使用 Server Component；只有需要状态、事件、`localStorage`、Canvas、Web Audio、震动或分享 API 时才使用 Client Component。
- 不在服务端渲染阶段直接访问 `window`、`document`、`navigator` 或 `localStorage`。
- 保持 `next.config.ts` 的静态导出兼容性。不要新增必须依赖 Next.js 服务端运行时的功能。
- 除非现有方案无法满足需求，否则不要增加依赖。新增依赖前说明用途、体积影响和纯前端兼容性。

## 4. 交互与数据规则

- 剂量仅允许：`2.5`、`5`、`7.5`、`10`、`12.5`、`15 mg`。
- 模拟部位仅允许：腹部 `abdomen`、大腿 `thigh`。
- 注射流程必须连续长按完整 15 秒才成功。
- 松手、触控取消、手指移出、页面失焦或流程中断时，进度立即归零。
- 中断不得保存记录，并显示：`注射中断！食欲正在反击`。
- 一次成功只能写入一条记录，避免重复事件或重复渲染造成多次保存。
- 同一天可以保存多条成功记录，但连续打卡按本地自然日只计算一次。
- 日期逻辑使用用户本地时区，不能用 UTC 日期直接代替 `YYYY-MM-DD` 本地日期。
- 历史记录继续使用带版本号的 `localStorage` 数据结构；读取损坏或未知版本数据时安全降级为空记录。
- 清空历史前必须二次确认，并明确提示删除不可恢复。

## 5. 浏览器能力与降级

- 对震动、系统分享、文件分享、音频和 Canvas 做功能检测，不通过设备名称猜测能力。
- 震动、声音和系统分享都属于可选增强，缺失时核心流程仍须可完成。
- iPhone 无网页震动时，使用机械声、像素动画和轻微画面抖动反馈。
- 音频必须由用户手势触发，并始终提供清晰的静音/恢复入口。
- 系统图片分享不可用时，提供保存图片的替代方式。
- API 调用失败时给用户明确、可恢复的反馈，不让页面停在无响应状态。

## 6. 视觉与可用性

- 保持 Kawaii 粉彩色系与 16-bit 像素游戏风，不改成写实医疗或黑底硬核街机风。
- 保持移动优先；桌面端使用居中的手机宽度容器即可。
- 适配 `safe-area-inset-*`，避免控件被刘海、动态岛或底部手势区域遮挡。
- 主要触控目标应足够大，适合单手操作。
- 选中、进度、中断和成功状态不能只靠颜色表达。
- 保持清晰的文字对比度和可见的键盘焦点样式。
- 尊重 `prefers-reduced-motion`；减少装饰性动画，但保留理解流程所需的进度反馈。
- 像素素材放大时保持锐利，使用 `image-rendering: pixelated` 或等效方案。
- 注射笔不能添加眼睛、嘴巴等拟人表情；人物服装必须完整，不展示裸露部位或局部特写。

## 7. 修改工作流

1. 阅读相关页面、组件、类型和 `SPEC.md` 对应章节。
2. 先确认现有实现和边界条件，再做最小、聚焦的修改。
3. 保留用户已有改动，不覆盖或回退无关文件。
4. 同步处理正常流程、错误流程、能力降级和无障碍状态。
5. 涉及本机存储、连续天数或 15 秒长按时，验证边界情况和重复触发。
6. 完成后运行与改动相称的检查，并如实说明尚未在真实设备验证的部分。

## 8. 必须执行的检查

常规代码修改完成后运行：

```bash
npm run lint
npm run typecheck
npm run build
```

针对关键功能还应检查：

- 15 秒前松手不会成功，也不会写入记录。
- 完整长按只生成一条记录。
- 刷新页面后本机历史仍存在。
- 同日多次记录只增加一个连续打卡日。
- 跨午夜、漏一天和设备本地时区下的连续天数正确。
- 不支持震动或系统分享时仍能完成核心流程。
- 分享图没有剂量、部位、日期或个人信息。
- 减少动态效果开启后仍能看懂进度和结果。
- 静态构建成功，且没有引入服务端依赖。

真实 iPhone Safari、Android Chrome、震动、系统分享和添加到主屏幕若未实际测试，必须明确标记为“未验证”，不能推测为已支持。

## 9. 完成标准

只有在以下条件满足时，才可以称任务已完成：

- 实现符合 `SPEC.md`，且没有越过娱乐产品和隐私边界。
- 核心流程在缺少可选浏览器能力时仍可使用。
- 类型检查、代码规范检查和生产构建通过。
- 没有新增不必要的后端、账号、远程数据上传或付费服务。
- 已说明实际验证范围、降级行为及任何仍待真实设备测试的内容。
